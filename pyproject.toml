[project]
name = "climate-closet-tooling"
version = "0.1.0"
description = "Typed helper tooling for the Climate Closet iOS repository."
requires-python = ">=3.9"

[tool.pyright]
include = ["scripts"]
pythonVersion = "3.9"
typeCheckingMode = "strict"
reportMissingTypeStubs = false

